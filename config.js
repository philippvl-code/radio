// IDs the studio can register under on the PeerJS server, in order of preference.
// If a stale studio tab (asleep, or open in another browser) still holds the first,
// the studio takes the next free one; viewers try them all.
window.STREAM_ID = "radio-93a5e592333b";
window.STREAM_IDS = [STREAM_ID, STREAM_ID + "-2", STREAM_ID + "-3", STREAM_ID + "-4"];
